vector vec = [102, 55, 43, 90, 5];
var i, min, j = 0, 0, 0;

for (; i < 5; i++) {
    min = i;
    j = i + 1;
    for (;j < 5; j++) {
        if (vec[j] < vec[min]) {
            min = j;
        }
    }
    swap(vec, i, min);
}

print (vec);