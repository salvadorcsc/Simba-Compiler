vector vec = [102, 55, 43, 90, 5];
var i, iter, j = 1, 0, 0;

for (; i < 5; i++) {
    j = i + 1;
    while (j != 0) {
        j - 1;
        iter = j - 1;
        if (vec[j] < vec[iter]) {
            swap(vec, iter, j);
        }
    }
}

print (vec);